[build]
  publish = "."
  functions = "netlify/functions"

# Serve assetlinks.json with correct content type (required for TWA)
[[headers]]
  for = "/.well-known/assetlinks.json"
  [headers.values]
    Content-Type = "application/json"
    Access-Control-Allow-Origin = "*"
